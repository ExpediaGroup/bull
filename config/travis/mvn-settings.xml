<settings>
  <servers>
    <server>
      <id>ossrh</id>
      <username>${env.EG_OSSRH_USERNAME}</username>
      <password>${env.EG_OSSRH_PASSWORD}</password>
    </server>
    <server>
      <id>sonatype-nexus-staging</id>
      <username>${env.EG_OSSRH_USERNAME}</username>
      <password>${env.EG_OSSRH_PASSWORD}</password>
    </server>
    <server>
      <id>sonatype-nexus-snapshots</id>
      <username>${env.EG_OSSRH_USERNAME}</username>
      <password>${env.EG_OSSRH_PASSWORD}</password>
    </server>
    <!-- GitHub config -->
    <server>
      <id>${github_server_id}</id>
      <password>${github_oauth_token}</password>
    </server>
  </servers>

  <profiles>
    <profile>
      <id>sign</id>
      <properties>
        <gpg.executable>gpg</gpg.executable>
        <gpg.keyname>${env.GPG_KEYNAME}</gpg.keyname>
        <gpg.passphrase>${env.GPG_PASSPHRASE}</gpg.passphrase>
      </properties>
    </profile>
  </profiles>
</settings>

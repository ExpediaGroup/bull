language: java
jdk:
  - oraclejdk8
os:
  - linux
cache:
  directories:
    - ~/.m2/repository
jobs:
  include:
    - stage: "Build"
      name: "Build"
      install: mvn clean install -DskipTests=true -Dmaven.javadoc.skip=true
#    - stage: "Test"
#      name: "Test"
#      install: mvn test -Dmaven.javadoc.skip=true
    - stage: "Quality Check"
      if: branch = master
      name: "Sonar check"
      install: mvn test sonar:sonar -Dmaven.javadoc.skip=true -Dsonar.projectKey=${sonar_project_key} -Dsonar.organization=${sonar_organization} -Dsonar.host.url=${sonar_host_url} -Dsonar.login=${sonar_token}
    - stage: "Deploy"
      deploy:
        provider: pages
        skip-cleanup: true
        github-token: $github_oauth_token
        keep-history: true
        on:
          branch: master
#    - stage: "Site Build"
#      name: "GitHub Site Build and Publish"
#      install: travis_wait 60 mvn clean install site --settings config/travis/mvn-settings.xml -DskipTests=true
#    - stage: "Deploy"
#      if: branch = master
#      name: "Deploy"
#      install: mvn test sonar:sonar -Dmaven.javadoc.skip=true -Dsonar.projectKey=${sonar_project_key} -Dsonar.organization=${sonar_organization} -Dsonar.host.url=${sonar_host_url} -Dsonar.login=${sonar_token}
